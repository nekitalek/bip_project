https:
    port: "8080"
    cert: ".certificate//cert.pem"
    key: ".certificate//key.pem"

db:
    username: "postgres"
    host: "localhost"
    port: "5432"
    dbname: "postgres"
    sslmode: "require"

smtp:
    from: "bip.project@rambler.ru"
    smtpHost: "smtp.rambler.ru"
    smtpPort: 587

template:
    EmailConfirmation: "htmlTemplate//template.html"


